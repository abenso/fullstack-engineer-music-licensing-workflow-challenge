# Copy this file to .env for local (non-Docker) runs.
# When running via `docker compose`, these are provided by compose.yml instead.

DATABASE_URL=postgres://licensing:licensing@localhost:5432/licensing
PORT=8080

# Log verbosity, e.g. "info", "debug", "music_licensing_backend=debug,tower_http=debug"
RUST_LOG=info
